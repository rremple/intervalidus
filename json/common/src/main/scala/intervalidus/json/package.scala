package intervalidus

/**
  * Supports an example Intervalidus application 's sample JSON pickling subprojects which could be useful when managing
  * Intervalidus data in a JSON data store (e.g., MongoDB) and/or serializing data through web services.
  */
package object json
