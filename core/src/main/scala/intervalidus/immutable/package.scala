package intervalidus

/**
  * Intervalidus's main traits and classes for defining immutable dimensional data, including:
  *   - Base traits for all immutable dimensional data, and all immutable versioned dimensional data.
  *   - Classes defining immutable data in one, two, and three dimensions
  *   - Classes defining immutable versioned data in one and two dimensions
  */
package object immutable
