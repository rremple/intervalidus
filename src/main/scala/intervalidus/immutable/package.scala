package intervalidus

/**
  * Intervalidus's main traits and classes for defining immutable dimensional data, including:
  *   - A base trait for all immutable dimensional data.
  *   - Classes for defining immutable data in one and two dimensions
  *   - Class for defining immutable versioned data in one dimension
  */
package object immutable
